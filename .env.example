# Runtime Environment Variables for Docker Container
# These variables are injected at container startup via docker-entrypoint.sh
# No need to rebuild the container when changing these values

# Backend API URL
VITE_API_URL=http://localhost:8000

# Enable debug logging
VITE_DEBUG=false
# Microsoft Clarity Project ID
VITE_CLARITY_ID=project-id
